<div class="col">
	<div id="{{brand-id}}-{{badge-type}}" class="brand-badge {{badge-type}} rounded-pill position-relative mb-2" role="button" brand-id={{brand-id}}>
		<div id="{{brand-id}}-{{badge-type}}-text" class="position-absolute top-50 start-50 translate-middle text-nowrap">
			<small>{{brand-name}}</small>
		</div>
	</div>
</div>